import { Routes } from '@angular/router';

/**
 * Defines the application's route configuration.
 * Currently, this array is empty but can be populated with route objects to handle different paths in the application.
 * @type {Routes}
 */
export const routes: Routes = [];
